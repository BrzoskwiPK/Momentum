import { useQuery } from '@tanstack/react-query'
import { FunctionComponent } from 'react'

const Home: FunctionComponent = () => {
  return <main className='w-full flex-grow flex items-center justify-center'>CONTENT TBD</main>
}

export default Home
